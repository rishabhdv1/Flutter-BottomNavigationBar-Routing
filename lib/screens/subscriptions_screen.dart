import 'package:flutter/material.dart';

class SubscriptionsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Subscriptions Screen'),
      ),
      body: Center(
        child: Text('Subscriptions Screen', style: TextStyle(fontSize: 24)),
      ),
    );
  }
}
